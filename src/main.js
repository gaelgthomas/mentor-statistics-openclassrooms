/**
 * This is entrypoint of the extension.
 * TAn Parser object is created.
 * TThe launchParsing method is called with page 1 as parameter.
 */

var parser = new Parser();

parser.launchParsing(1);
